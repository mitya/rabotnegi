- page "admin-employers", "Работодатели"

= tlist @models do
  thead
    tr
      th Название
      th Логин
      th
  - for model in @models
    tr{class=cycle('', 'alt')}
      td= model.name
      td= model.login
